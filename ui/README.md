# ui

Reusable UI components for the application.

> The context-menu system previously housed here has been extracted to individual application implementations. Each application now owns its own context-menu components and styles.
